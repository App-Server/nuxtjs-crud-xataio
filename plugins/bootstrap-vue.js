import Vue from 'vue';
import { BootstrapVue, IconsPlugin } from 'bootstrap-vue';

// Instalar o BootstrapVue
Vue.use(BootstrapVue);
// Opcionalmente, instalar os ícones do BootstrapVue
//Vue.use(IconsPlugin);
